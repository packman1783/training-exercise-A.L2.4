package com.example.spring.model;

// It is necessary to introduce a general basic interface for all models
// on which Mappper could navigate and understand, apply the toEntity () method

public interface BaseEntity {
}
